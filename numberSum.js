const arr = [1, 2, 3, 4] // 10

// console.log(arr.map((el) => el * 2))

// function makeSum(array) {
//   let sum = 0;
//   for (let i = 0; i < array.length; i++) {
//     sum = sum + array[i];
//   }
//   return sum;
// }

// const makeSum = (array) => array.reduce((acc, el) => acc + el, 0)
// acc - 0, element - 1 -> acc = acc + element = 1
// acc - 1, element - 2 -> acc = acc + element = 3
// ...

